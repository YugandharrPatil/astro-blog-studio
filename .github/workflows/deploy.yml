name: Trigger site rebuild

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Astro Deploy
        run: |
          curl -X POST ${{ secrets.DEPLOY_WEBHOOK }}
